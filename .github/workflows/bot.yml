name: SIP Table Scraper

on:
  workflow_dispatch:
    inputs:
      page_url:
        description: "Page URL to scrape"
        required: true

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Setup Python
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # Step 3: Install system dependencies
      - name: Install Chromium and ChromeDriver
        run: |
          sudo apt-get update
          sudo apt-get install -y chromium-browser chromium-chromedriver libnss3

      # Step 4: Verify installations
      - name: Verify Chromium and Chromedriver
        run: |
          echo "Chromium version:"
          chromium-browser --version
          which chromium-browser
          echo "Chromedriver version:"
          chromedriver --version
          which chromedriver

      # Step 5: Install Python packages
      - name: Install Python packages
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # Step 6: Run main.py
      - name: Run main.py
        env:
          PAGE_URL: ${{ github.event.inputs.page_url }}
        run: python main.py

      # Step 7: Commit changes if table.md updated
      - name: Commit changes
        uses: lazarevicOgnjen/git-auto-commit-action@v6
        with:
          commit_message: "Update table.md"
          file_pattern: table.md
        id: auto-commit-action
